'use strict';

// 兼容保留的废弃模块：canonical capability authority 仅由 demoPostActionService 闭包持有。
const deprecatedCapabilitySurface = Object.freeze({});

Object.defineProperty(module, 'exports', {
  value: deprecatedCapabilitySurface,
  enumerable: true,
  writable: false,
  configurable: false
});
